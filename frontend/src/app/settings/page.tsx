import { Metadata } from "next";
import React from "react";

export const metadata: Metadata = {
  title: "Settings Page",
  description: "Here you can find your settings",
};

export default function SettingsPage() {
  return <div>settings</div>;
}
